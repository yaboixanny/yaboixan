# Exclude API calls and static assets from the global rewrite
/api/*      /404            404
/images/*   /images/:splat  200
/posts.json /posts.json     200

# Admin and local files
/admin      /admin.html     200
/index      /index.html     200

# Rewrite all other paths to post.html to handle slugs
/*          /post.html      200
